# Reglas de Afiliación — CryptoLucky (detalle)
> Cargado bajo demanda por las skills `cryptolucky-seo` y `cryptolucky-compliance`.

## Enlaces de afiliado
- **NO CONFUNDIR (distinción crítica):**
  - **Enlace/URL de referido** (va en CTAs, botones, `href`): `https://betfury.io/?r=LUCKYSIRKO007`
  - **Código de referido** (texto que el usuario introduce al registrarse): `LUCKYSIRKO007`
  - El enlace YA lleva el código incrustado (`?r=LUCKYSIRKO007`). En un botón se usa el ENLACE; cuando se muestra el código como texto para copiar, se usa el CÓDIGO.
- TODO enlace de afiliado lleva `rel="sponsored nofollow"` (práctica más defensiva ante Google).
- Disclosure textual VISIBLE en la página junto al enlace o al inicio del artículo
  (no basta el atributo HTML): requisito legal en ES/EU.
- Rutas de redirect/tracking (ej. `/go/exchange-x/`) con `noindex` y excluidas en `robots.ts`.
- El link de afiliado va en TODOS los CTAs del blog.

## Buenas prácticas
- No prometer rentabilidad ni "dinero seguro".
- Comparativas con criterios explícitos y metodología (mejora GEO y confianza).
- Mezcla equilibrada: pocos enlaces externos de calidad + enlaces internos bien distribuidos (3-8 por artículo).
