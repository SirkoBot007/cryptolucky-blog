# Sesión 2026-06-25

## Objetivo
Activar el cerebro en `main` y empezar la organización del workspace (plan `PLAN-ORGANIZACION-Y-CEREBRO.html`, aprobado por el CEO).

## Hecho
- **Logo de marca:** integrado (favicon, apple-icon, OG por defecto, cabecera). Versión **transparente** (recorte rembg, sin halo) en cabecera + favicon. Desplegado a producción (commits hasta `23c5f0e`).
- **Corrupción `articles-new.ts`:** eliminado un Chunk B huérfano (borrador Panamá duplicado fuera del array); working tree válido (tsc+build OK). El artículo `betfury-casino-panama` real ya estaba en HEAD (status Archivado) — recuperable/publicable.
- **Limpieza:** eliminados `seo_geo_expand.py` y `seo_geo_expand2.py` (scripts de un solo uso). Commit `539280c`.
- **Cerebro activado en `main` (Fase 1):** traídos `.claude/rules` (18) + `.claude/skills` (6) + `CLAUDE.md` desde la rama `release/hreflang-xdefault-2026-06-23` (commit `59b6817`), SIN mergear el resto. Creado `.claude/memory/` (índice `MEMORY.md`, `preferencias-ceo.md`, sesiones) y la regla `organizacion-continua.md`.

## Decisiones / notas
- Brand: logo transparente en cabecera+favicon; con fondo en apple-icon (iOS) y OG.
- Las reglas/skills vivían solo en una rama local sin pushear → de ahí la "discrepancia del cerebro fantasma". Ya en `main`.

## Calidad SEO de artículos (norma + plantilla)
- **Nueva regla** `.claude/rules/calidad-seo-articulos.md` (anti thin content): método obligatorio por artículo (investigar→GEO→≥1.200 pal→FAQ→enlazado→E-E-A-T→compliance). Referenciada en `CLAUDE.md` e indexada. Commit `bd786e5` (pusheado).
- **Auditoría 54 artículos:** 33 críticos-thin (<600), 14 débiles, 5 justos, 2 OK. Mediana 515 pal → explica la mala indexación. Lista priorizada entregada al CEO.
- **Plantilla reescrita:** `casino-cripto-colombia-mejores` 415→1.287 pal. **Aprobada por el CEO y desplegada** (`f26a7ae`).
- **Maratón de reescritura en marcha.** Tracker reanudable: `BLOG-BETFURY/docs/PROGRESO-REESCRITURA.md`.
  - **Alcance reducido (CEO):** solo thin/no-indexados. SKIP los ≥~1.000 pal o ya indexados (7 SKIP). Total en alcance: 47; **hechos 12, pendientes 35**.
  - **Lote 1** (`ecea9fb`): mejores-casinos-criptomonedas, que-es-casino-cripto, casino-cripto-legal-latam, casino-bitcoin-sin-kyc, betfury-vs-stake, token-bfg → ~1000-1130 pal.
  - **Lote 2** (`a9c18ef`): casino-cripto-mexico-mejores, betfury-bono-sin-deposito-free-spins, betfury-codigo-promocional-luckysirko007, betfury-casino-argentina, betfury-casino-peru → 1037-1147 pal.
  - Indexación SC la hace el CEO. Siguiente: P3 restante (venezuela, chile, bolivia, ecuador, panama, espana) + P4.

## Lección aprendida (calidad SEO)
- Los **primeros borradores** de reescritura salieron a ~800 pal (por debajo del estándar ≥1.200). **Solución:** escribir más largo de inicio (como la plantilla Colombia, 1.287) o **expandir** con secciones útiles antes de la conclusión. Herramientas reutilizables en `Temp/`: `rewriter.py` (reemplaza content por slug, sin regenerar array) + `inserter.py` (inserta secciones antes de "## Conclusión"). **Edición quirúrgica fiable y barata en tokens** (no relee el array). Cuidado: en heredocs, `\\` se colapsa → escribir los scripts con la herramienta Write, no con heredoc.

## Pendiente (siguiente)
- **Fase 2:** desaturar la raíz del workspace (mover docs CryptoLucky a la línea del blog), consolidar duplicados (.md/.html, drafts -FINAL, brand/branding, n8n ×2), retirar `.bat`, archivar obsoletos (sin borrar). Crear `INDICE.md` raíz.
- **WIP del CEO sin tocar:** `app/[locale]/betfury/page.tsx`, `app/[locale]/page.tsx`.

## Candidatos a memoria (promovidos)
- Preferencias CEO → `preferencias-ceo.md` (creado).
