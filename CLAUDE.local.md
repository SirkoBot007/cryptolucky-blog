# CLAUDE.local.md — Overrides locales (NO se sube a GitHub)
> Este archivo está en `.gitignore`. Aquí van datos sensibles, overrides del CEO y notas personales.
> NUNCA pegar aquí claves reales si este repo pudiera sincronizarse; usar `.env.local` para secretos de runtime.

## 🔑 Referencias (no pegar valores reales)
- Variables de entorno reales → viven en `.env.local` y en Vercel (Project Settings).
- Claves GCP / GA4 service account → centralizadas en `_SECRETOS/` del workspace, NUNCA aquí ni en el repo.

## 👤 Overrides del CEO (Alberto / Sirko007)
- El usuario NO ejecuta comandos: Claude hace todo, el CEO verifica y aprueba.
- Preguntar antes de actuar en cambios irreversibles; si el CEO dice no, proponer alternativa.
- Respuestas en español, tono de agencia profesional.

## 📝 Notas de sesión personales
- (libre)
